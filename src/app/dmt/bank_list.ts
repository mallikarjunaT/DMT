export interface bankList {
    id: number;
    bankName: string;
}
export interface stateList {
    id: number;
    stateName: string;
}

export interface bankTransferMode {
    id: number;
    mode: string;
}